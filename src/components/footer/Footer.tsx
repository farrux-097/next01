import { memo } from 'react';

const Footer = () => {
  return (
        <>
            <footer className='w-full h-[50px] bg-gray-300 fixed bottom-0 right-0 left-0'>

            </footer>
        </>
  );
};

export default memo(Footer);